# PRD（产品需求文档）—— 个人工作台（双端 + 后端）

- 产品名称：个人工作台（Personal Workbench）
- 版本：v2.0
- 日期：2026-08-04
- 作者：WorkBuddy
- 目标用户：不太懂代码的个人用户（C 端）；需要运营/管理内容的运营人员（B 端）

## 1. 产品概述
### 1.1 背景
用户希望有一个属于自己的「个人工作台」，既能当**手机 App 日常用**（任务/笔记/知识库/内容/商城），又能有一个**后台管理系统**来发布内容、管理用户与配置。数据隐私优先，不愿明文上网。

### 1.2 目标
打造一个**本地优先、跨设备、隐私安全**的双端产品：
- **C 端（手机 App）**：年轻化移动界面，本地加密存储，可选账号云同步；内容消费 + 工具效率 + 交易服务。
- **B 端（管理系统）**：专业后台，运营发布内容、管理用户/角色/租户、查看数据看板与审计。
- **后端服务**：真实 Node 服务，负责账号鉴权、内容/订单/同步接口，端到端加密（密码不离开浏览器）。

### 1.3 范围（本期）
| 端 | 模块 | 本期 | 说明 |
|---|---|---|---|
| C 端 | 看板 | ✅ | 核心 |
| C 端 | 任务待办 | ✅ | 核心 |
| C 端 | 笔记资料 | ✅ | 核心（Markdown） |
| C 端 | 知识库 | ✅ | 核心 |
| C 端 | 内容消费 | ✅ | 新增（B 端发布，C 端浏览） |
| C 端 | 交易服务（商城） | ✅ | 新增（骨架，mock 支付） |
| C 端 | 我的（个人中心+配置+账号同步） | ✅ | 核心 |
| B 端 | 数据看板 | ✅ | 核心 |
| B 端 | 内容管理（CRUD） | ✅ | 核心 |
| B 端 | 用户/角色/租户 | ✅ | 核心（RBAC） |
| B 端 | 站点配置/健康/审计 | ✅ | 核心 |
| 后端 | 鉴权/内容/订单/同步 API | ✅ | 核心 |
| 多端实时推送 | ❌ | 超范围 |

## 2. 用户画像与场景
- C 端画像：个人用户，非技术背景，手机为主；要轻量、好看好上手。
- B 端画像：运营/管理员，桌面办公；要专业、表格化、可批量。
- 场景 A（手机）：通勤看今日待办、随手记灵感、逛内容、下单。
- 场景 B（电脑 B 端）：发布文章、管理用户、看运营数据。
- 场景 C（跨设备）：手机记的笔记，电脑浏览器接着看（靠账号云同步）。

## 3. 功能需求
> P0=必须有，P1=重要，P2=增强

### FR-01 任务待办（C 端 P0）
- 新增/编辑/删除任务，字段：标题、**备注**、截止日期、状态（待办/进行中/完成）、分类标签。
- 看板/列表视图；看板显示「今日待办」「已逾期」。
- 打开时对「已逾期/今天到期」做站内提示。
- 验收：增删改查并正确分类、统计；备注可编辑。

### FR-02 笔记资料（C 端 P0）
- 新建/编辑/删除笔记，支持 Markdown 排版（marked.js 渲染 + 目录 TOC）。
- 标签分类；可保存网页链接（自动生成可点链接）。
- 全文搜索；显示最近编辑/最近笔记列表。
- 验收：Markdown 正确渲染，搜索命中，链接可点开。

### FR-03 数据看板（C 端 P0 / B 端 P0）
- C 端：一屏汇总今日任务、最近笔记、习惯打卡；近 7 日趋势；逾期/今日到期站内提示。
- B 端：运营统计卡（内容数/用户数/订单数）+ 趋势折线 + 状态环形图（Chart.js）。
- 验收：数据随模块实时更新，图表正确。

### FR-04 知识库（C 端 P0）
- 结构化知识卡片：标题、摘要、正文、分类、标签。
- 卡片网格 + 详情；一键复制正文；分类/标签/全文搜索。

### FR-05 内容消费（C 端 P0，来自 B 端发布）
- 浏览/搜索/按分类筛选 B 端发布的**已发布**内容；收藏（本地）。
- 详情 Markdown 渲染；公开，不强制本地解锁。
- 验收：C 端能读 B 端发布的内容，搜索/分类/收藏正常。

### FR-06 交易服务（C 端 P1，骨架）
- 商品橱窗 → 购物车 → 模拟结算（下单即视为已支付 mock）→ 我的订单（账号归属）。
- 未登录引导去「我的」登录。
- 验收：登录后可下单、查看自己订单；支付为 mock。

### FR-07 我的（C 端 P0）
- 首次设访问密码（密码门，未解锁不显示数据）。
- 个人中心：头像首字母、用户名/角色、同步状态。
- **账号云同步**：登录/注册账号 → 开启云同步，加密数据跨设备合并（替换原 GitHub 同步）。
- 访问控制：修改密码、锁定退出。
- 数据备份：导出/导入 JSON。
- 网页端配置：站点名、深浅主题、模块显隐/改名/排序、默认预设、看板布局。
- 快捷入口卡片：知识库、商城。

### FR-08 B 端管理（P0）
- 登录/注册（JWT）；管理员看板。
- 内容管理：发布/编辑/下架内容（标题/摘要/正文/分类/标签/状态）。
- 访问控制：用户、角色（权限）、租户 CRUD（RBAC）。
- 运维：站点配置、服务健康、审计日志。
- 验收：管理员可完成上述全部操作；非管理员被拦截。

### FR-09 后端服务（P0）
- 鉴权：登录/注册/改密/登出（JWT + bcrypt 密码哈希）。
- 内容接口：公开读（已发布）、管理员写。
- 订单接口：下单（mock 支付）、我的订单（用户归属）。
- 同步接口：按用户存储加密 blob（端到端加密，服务端只见密文）。
- 验收：健康、鉴权、内容、订单、同步接口全部可用；源码目录不被静态托管泄露。

## 4. 非功能需求
- 兼容性：主流桌面/手机浏览器；B 端面向桌面，C 端面向手机。
- 性能：本地操作无感延迟；首屏 < 2s（含 CDN）。
- 安全隐私：**端到端加密**——访问密码只在浏览器用于加/解密，后端只见密文 blob；账号令牌仅作传输凭证。
- 可用性：C 端大字号大按钮；B 端表格化清晰层级。
- 离线：C 端断网可用本地数据（PWA）；后端需在线。
- 可维护：C 端纯静态无构建；B 端/CDN 化；后端零原生依赖（可换 SQLite）。

## 5. 关键用户流程
- C 端首次：打开 → 设密码 →（可选）注册账号开云同步 → 进入工作台。
- C 端日常：打开 → 输密码解锁 → 操作 → 本地保存（+ 自动加密上传同步）。
- B 端运营：登录 → 发布内容/管用户 → C 端用户可见。
- 跨设备：A 改动 → 加密上传后端 → B 登录同一账号拉取解密合并。

## 6. 验收标准
- [x] C 端六大模块可用（增删改查/搜索/渲染/收藏/下单）。
- [x] C 端密码门生效，错误密码无法看数据。
- [x] 后端账号云同步在双设备一致（端到端加密、无明文泄露）。
- [x] B 端可发布内容并被 C 端读取；RBAC 拦截非管理员。
- [x] 断网时 C 端本地功能正常；恢复后同步正常。
- [x] 电脑（B 端）与手机（C 端）视图均可用、不串版。
- [x] 导出/导入 JSON 可完整备份恢复。
- [x] README / PRD / 技术栈文档 / 迁移指南随交付并可读。

## 7. UI 与风格（双端区分）
- **C 端（手机 App）**：移动优先、卡片化、年轻活泼；底部固定标签栏（看板/内容/任务/知识库/我的），侧边栏隐藏；浅色/深色可切换；颜色来自 `tokens.css` 设计令牌。
- **B 端（管理系统）**：桌面侧边栏 + 顶栏 + 内容区；中性专业配色、表格化、清晰层级；浅色/深色可切换（`admin.css`）。
- 两端均不使用任何受版权保护的角色/吉祥物形象；站头以中性几何标识点缀。
- C 端配置项（站点名/主题/模块/预设/看板布局）在「我的」页网页端调整，无需改代码。
