# PRD（产品需求文档）—— 个人工作台

- 产品名称：个人工作台（Personal Workbench）
- 版本：v1.0
- 日期：2026-08-04
- 作者：WorkBuddy
- 目标用户：不太懂代码的个人用户

## 1. 产品概述
### 1.1 背景
用户希望有一个属于自己的「个人工作台」，集中管理日常任务、笔记、习惯与数据，电脑和手机都能用，并重视数据隐私、不愿明文上网。

### 1.2 目标
打造一个**本地优先、跨设备、隐私安全**的个人工作台：电脑/手机可用；四大模块一站式；数据默认本地、可选加密同步；小白可上手、可微调。

### 1.3 范围（本期）
| 模块 | 本期 | 说明 |
|---|---|---|
| 任务待办 | ✅ | 核心 |
| 笔记资料 | ✅ | 核心 |
| 数据看板 | ✅ | 核心 |
| 知识库 | ✅ | 核心 |
| 我的（个人中心+配置） | ✅ | 核心 |
| 用户系统/多人协作 | ❌ | 超范围 |
| 后端服务器/系统推送 | ❌ | 超范围 |

## 2. 用户画像与场景
- 画像：个人用户，非技术背景，会基础电脑/手机操作，但不写代码。
- 场景 A（电脑）：白天记任务、写笔记、查代码片段。
- 场景 B（手机）：通勤看今日待办、随手记灵感、打卡习惯。
- 场景 C（跨设备）：电脑记的笔记，手机接着看（靠云同步）。

## 3. 功能需求
> P0=必须有，P1=重要，P2=增强

### FR-01 任务待办（P0）
- 新增/编辑/删除任务。
- 字段：标题、备注、截止日期、状态（待办/进行中/完成）、分类标签。
- 看板/列表视图；首页显示「今日待办」「已逾期」。
- 打开网站时对「已逾期/今天到期」做站内提示。
- 验收：能完整增删改查并正确分类、统计。

### FR-02 笔记资料（P0）
- 新建/编辑/删除笔记，支持 Markdown 简单排版。
- 标签分类；可保存网页链接。
- 全文搜索；显示最近编辑列表。
- 验收：Markdown 正确渲染，搜索命中，链接可点开。

### FR-03 数据看板（P0）
- 一屏汇总：今日任务、最近笔记、习惯打卡日历。
- 简单图表：本周完成任务数、连续打卡天数（Chart.js）。
- 验收：数据随其他模块实时更新，图表正确显示。

### FR-04 知识库（P0）
- 结构化知识卡片：标题、摘要、正文、分类、标签。
- 列表卡片网格展示，点开看详情；支持一键复制正文。
- 按分类 / 标签 / 全文（标题·摘要·正文）搜索。
- 分类可在「我的 → 默认预设」中增删。
- 验收：能新增/编辑/复制/搜索，分类与标签筛选正确。

### FR-05 我的（个人中心 + 全部配置）（P0）
- 首次使用设访问密码（密码门，未解锁不显示数据）。
- 个人中心：头像首字母、用户名/角色、同步状态。
- 访问控制：修改密码、锁定退出。
- 数据同步：GitHub 同步（用户名/仓库/令牌/路径），测试同步与拉取同步。
- 数据备份：导出/导入 JSON。
- 网页端配置（无需改代码）：站点名、深浅主题与配色（实时预览）、模块显隐/改名/排序、默认预设（任务标签/习惯项/知识库分类）、看板布局。
- 验收：设密码后可解锁；配置改动即时生效并自动保存；填 GitHub 后能加密同步；导出文件可再导入恢复。

## 4. 非功能需求
- 兼容性：主流桌面/手机浏览器（Chrome/Edge/Safari/微信内置浏览器）。
- 性能：本地操作无感知延迟；首屏 < 2s（含 CDN）。
- 安全隐私：数据 AES 加密后存储；密码不离开浏览器；令牌仅存本机。
- 可用性：大字号、大按钮、步骤少；关键操作有提示。
- 离线：断网可正常使用本地数据（PWA 缓存）。
- 可维护：纯静态、无构建步骤，用户可改文字/样式。

## 5. 关键用户流程
- 首次：打开 → 设密码 →（可选）配置 GitHub → 进入工作台。
- 日常：打开 → 输密码解锁 → 操作 → 自动保存到本地。
- 跨设备：A 改动 → 加密上传 GitHub → B 打开 → 拉取解密合并。

## 6. 验收标准
- [x] 四大模块均可正常使用（增删改查/搜索/渲染）。
- [x] 密码门生效，错误密码无法看数据。
- [x] GitHub 同步在双设备间一致（加密、无明文泄露）。
- [x] 断网时本地功能正常；恢复后同步正常。
- [x] 电脑与手机视图均可用、不串版。
- [x] 导出/导入 JSON 可完整备份恢复。
- [x] README / PRD / 技术栈文档随交付并可读。

## 7. UI 与风格（专业管理后台）
- 左固定侧边栏 + 顶栏 + 内容区布局；手机端自动切换为顶栏 + 底部标签栏。
- 中性专业配色：深蓝/藏青主色，收敛的辅助色；**浅色 / 深色主题可一键切换**。
- 表格化信息展示（任务/笔记），知识库用卡片网格；清晰的信息层级与大点按区，小白友好。
- 站头/空状态以中性几何标识点缀，**不使用任何受版权保护的动画角色或吉祥物形象**。
- 配置项（站点名、主题色、模块、预设、看板布局）均可在「我的」页网页端调整，无需改代码。
