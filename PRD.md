# PRD（产品需求文档）—— 个人工作台（纯前端 v2.4）

- 产品名称：个人工作台（Personal Workbench）
- 版本：v2.4（纯前端）
- 日期：2026-08-05
- 作者：WorkBuddy
- 目标用户：不太懂代码的个人用户（C 端）；需要查看数据/管理配置的个人使用者（B 端）

## 0. 版本演进（摘要）
- **v2.1（纯前端）**：移除 Node 后端与内容/交易模块，改为纯静态 + 用户自有 GitHub 仓库端到端加密云同步。
- **v2.2（C 端界面优化）**：修复导航 Bug，重构习惯/知识库/财政/设置模块，统一移动端基础体验。
- **v2.3（可视化与双端协同）**：C 端看板/习惯/综合/财政的可视化与排版升级（整合月历 + 年度热力图 + 多元图表、顶部子 tab、窄屏表格转卡片、财政手机端重构）；管理台新增 C 端 ⇄ 管理台无缝免密切换、响应式侧栏、近 6 月财政收支图与财政账户预设编辑。
- **v2.4（财政重设计 + 文档对齐）**：财政模块重设计——账户体系（自定义账户：支付宝/微信/银行卡·具体到行/现金/其他，含初始余额与实时余额追踪）、趋势日/月/年切换（日=当月每天折线、月=当年 12 月柱状、年=逐年柱状）、新增/编辑表单改为底部弹窗两列网格 + 收入/支出分段切换（解决原单列冗长）；进阶：概览账户收支汇总卡片、明细按账户筛选/分组、攒钱关联账户。同时补齐此前 commit 的文档：移动端固定外壳修复（底部五图标栏钉死、内容区内部滚动、杜绝横向溢出）与 C 端知识库下架（配置/导航/统计/页面/脚本全链路清除）。

## 1. 产品概述
### 1.1 背景
用户希望有一个属于自己的「个人工作台」，能当**手机 App 日常用**（任务/笔记/知识库），并有一个**本地管理控制台**查看数据、管理配置。数据隐私优先，不愿明文上网；且希望纯静态部署（如 GitHub Pages）即可跨设备使用。

### 1.2 目标
打造一个**本地优先、跨设备、隐私安全**的纯前端产品：
- **C 端（手机 App）**：年轻化移动界面，本地加密存储，可选 GitHub 云端同步；工具效率为主。
- **B 端（管理控制台）**：桌面浏览器本地控制台，复用同一份加密数据，提供数据概览、备份/恢复、云同步、站点与模块配置。
- **零后端**：不依赖任何服务器；跨设备同步通过用户自己的 GitHub 仓库（端到端加密 blob）实现。

### 1.3 范围（本期）
| 端 | 模块 | 本期 | 说明 |
|---|---|---|---|
| C 端 | 看板 | ✅ | 核心 |
| C 端 | 任务待办 | ✅ | 核心 |
| C 端 | 笔记资料 | ✅ | 核心（Markdown） |
| C 端 | 知识库 | ❌ | v2.4 起已从 C 端移除（配置/导航/统计/页面/脚本全链路清除） |
| C 端 | 我的（个人中心+配置+GitHub 同步） | ✅ | 核心 |
| B 端 | 数据概览 | ✅ | 核心 |
| B 端 | 数据管理（备份/恢复/云同步） | ✅ | 核心 |
| B 端 | 站点与模块配置 | ✅ | 核心 |
| 后端/服务器 | ❌ | 本期移除 | 改为纯前端 + GitHub 仓库同步 |
| 内容发布/交易服务 | ❌ | 本期移除 | 回归个人工作台核心 |

## 2. 用户画像与场景
- C 端画像：个人用户，非技术背景，手机为主；要轻量、好看好上手。
- B 端画像：同一使用者，桌面办公；要看统计、管理配置、备份数据。
- 场景 A（手机）：通勤看今日待办、随手记灵感、查知识卡片。
- 场景 B（电脑管理台）：看数据概览、改站点名/主题、备份恢复。
- 场景 C（跨设备）：手机记的笔记，电脑/另一台手机登录同一 GitHub 仓库 + 同一密码即可拉取。

## 3. 功能需求
> P0=必须有，P1=重要，P2=增强

### FR-01 任务待办（C 端 P0）
- 新增/编辑/删除任务，字段：标题、备注、截止日期、状态（待办/进行中/完成）、分类标签。
- 看板/列表视图；看板显示「今日待办」「已逾期」。
- 打开时对「已逾期/今天到期」做站内提示。
- 验收：增删改查并正确分类、统计；备注可编辑。

### FR-02 笔记资料（C 端 P0）
- 新建/编辑/删除笔记，支持 Markdown 排版（marked.js 渲染 + 目录 TOC）。
- 标签分类；可保存网页链接（自动生成可点链接）。
- 全文搜索；显示最近编辑/最近笔记列表。
- 验收：Markdown 正确渲染，搜索命中，链接可点开。

### FR-03 数据看板（C 端 P0 / B 端 P0）
- C 端：看板顶部四大概览改为渐变 Hero + 统计胶囊；通过「日历 / 图表 / 列表」子 tab 组织多视图——
  - 日历：整合月历（任务截止蓝、笔记更新紫、习惯打卡绿）+ 年度热力图；
  - 图表：近 7 日完成折线、任务状态环形、近 6 月收支柱状；
  - 列表：最近任务 / 笔记 / 习惯连续汇总。
- B 端：统计卡（任务/笔记/知识库/习惯 + 完成率）+ 近 7 日趋势折线 + 模块分布环形图 + **近 6 月财政收支柱状图**（Chart.js）。
- 验收：数据随模块实时更新，图表正确；窄屏下图表与表格自适应。

### FR-04 知识库（C 端 P0）
- 结构化知识卡片：标题、摘要、正文、分类、标签。
- 卡片网格 + 详情；一键复制正文；分类/标签/全文搜索。

### FR-05 我的（C 端 P0）
- 首次设访问密码（密码门，未解锁不显示数据）。
- 个人中心：用户名/角色、同步状态。
- **GitHub 云端同步**：填写 PAT + 仓库信息 → 测试/推送/拉取，加密数据跨设备合并。
- 访问控制：修改密码、锁定退出。
- 数据备份：导出/导入 JSON。
- 网页端配置：站点名、深浅主题、模块显隐/改名/排序、默认预设（任务标签 / 习惯项 / 财政分类 / **账户管理（支付宝/微信/银行卡·具体到行/现金/其他，含初始余额）**）、看板布局。
- 快捷入口卡片：管理台（点此免密进入 B 端）。
- 习惯打卡可视化：月历网格（点格打卡/取消）+ 年度热力图双视图，可切换。

### FR-06 B 端管理控制台（P0）
- 本地解锁（与 C 端同一访问密码）。
- **C 端 ⇄ 管理台无缝切换**：B 端顶栏「← 工作台」与左侧「工作台（C 端）」入口可免密返回 C 端（共用同一会话密钥），无需重新输密码。
- **响应式侧栏**：桌面端「☰」折叠成仅图标的窄条；手机端以抽屉滑出 + 遮罩，适配小屏。
- 数据概览：本地统计卡 + 图表（近 7 日趋势折线、模块分布环形图、**近 6 月财政收支柱状图**）。
- 数据管理：导出/导入备份、GitHub 云同步（推送/拉取/测试）、危险区（清空本地数据）。
- 站点配置：站点名称、深浅主题、模块显隐开关、**财政分类与账户管理（自定义账户：类型/名称/银行/初始余额）**（写回同一份配置）。
- 关于：版本号、技术栈说明、本地存储占用。
- 验收：管理台与 C 端读写同一份加密数据，改动互通；两端间可免密切换。

### FR-08 财政模块重设计（C 端 P0）
> 背景：原财政仅「自由文本账户 + 近 6 月柱状图 + 单列冗长表单」，无法满足细致记账与多账户管理。本期重设计账户体系、趋势粒度与表单形态。

**8.1 账户体系（自定义账户管理）**
- 账户数据结构：`{ id, type, name, bank?, initialBalance? }`
  - `type` ∈ {支付宝, 微信, 银行卡, 现金, 其他}；
  - `bank`：仅当 `type=银行卡` 时填写具体银行（如「招商银行」「工行信用卡」）；
  - `initialBalance`：账户初始余额（默认 0）。
- 入口：设置页「账户管理」卡片列表，支持新增/编辑/删除账户（弹窗表单：类型下拉 → 名称 → 银行(仅 type=银行卡 显示) → 初始余额）。
- 预置账户（`defaults.accounts`）：支付宝、微信、招商银行（示例）、现金（用户可按需删改）。
- 记账时「账户」改为从账户列表下拉选择，写入记录的 `accountId`；旧文本账户自动迁移为「现金/其他」类账户。
- 兼容：旧的 `financeAccounts` 字符串数组在 `migrateConfig` 中转为账户对象；旧 `finance[].account` 文本迁移为关联同名/默认账户或记为「未分类」。

**8.2 趋势日/月/年切换**
- 趋势子 tab 顶部三个分段按钮：日 / 月 / 年。
- 日视图：折线图，显示所选月份每天的「收入 / 支出」两条线；可左右切换月份。
- 月视图：柱状图，显示所选年份 12 个月的收支；可左右切换年份。
- 年视图：柱状图，显示近 5 年每年的收支汇总；可切换年份区间。
- 顶部筛选的「月份」输入在日/月视图复用为锚定月份/年份；年视图提供年份切换。

**8.3 新增/编辑表单（紧凑化）**
- 统一改为底部弹窗（modal-sheet）+ 两列网格（`.form-grid`）：
  - 顶部「收入 / 支出」分段切换（`.seg`）；
  - 金额：大号输入框（numeric，醒目）；
  - 网格行 `[分类 | 账户]` 并排（均为下拉）；
  - 网格行 `[日期 | （占位）]`；
  - 备注：默认隐藏，点「添加备注」展开（减少纵向冗长）。
- 同样紧凑化「记攒钱」「新增目标」表单（金额大号 + 网格 + 备注折叠）。

**8.4 进阶功能**
- 账户收支汇总卡片（概览）：在四大概览统计下，新增「账户」卡片行，每个账户显示类型图标 + 名称 + 当月收入/支出/实时余额（余额 = 初始余额 + 收入 − 支出）。
- 余额追踪：账户实时余额随记账自动更新；概览与账户详情均可查看。
- 明细按账户筛选/分组：顶部筛选新增「账户」下拉；明细列表支持按账户过滤，并可切换「按账户分组」视图。
- 攒钱关联账户：攒钱记录新增 `accountId`，记攒钱时可选择来源账户；攒钱列表显示对应账户。

**8.5 验收**
- 账户可增删改；记账选账户并正确计入余额；
- 趋势日/月/年三种粒度切换正确、图表无错位；
- 表单在手机窄屏两列不溢出、观感紧凑；
- 明细可按账户筛选/分组；攒钱关联账户正确显示；
- 旧数据（文本账户 / 字符串账户预设）迁移无报错。

### FR-07 端到端加密与云同步（P0）
- 加密：访问密码 → PBKDF2 → AES-GCM 密钥；本地只存密文 blob，密码不落盘、不上传。
- GitHub 同步：加密 blob `PUT/GET` 到用户仓库文件；PAT 仅作传输凭证；冲突以 `updatedAt` 最新者为准。
- 验收：双设备用同一仓库 + 同一密码可合并；GitHub 仓库只见密文。

## 4. 非功能需求
- 兼容性：主流桌面/手机浏览器；B 端面向桌面，C 端面向手机。
- 性能：本地操作无感延迟；首屏 < 2s（含 CDN）。
- 安全隐私：**端到端加密**——访问密码只在浏览器用于加/解密，GitHub 只见密文 blob；PAT 仅作传输凭证。
- 可用性：C 端大字号大按钮；B 端表格化清晰层级。
- 离线：C 端断网可用本地数据（PWA）。
- 可维护：纯静态无构建；用户可改文字/样式/配置，无需服务器。

## 5. 关键用户流程
- C 端首次：打开 → 设密码 →（可选）配置 GitHub 同步 → 进入工作台。
- C 端日常：打开 → 输密码解锁 → 操作 → 本地保存（+ 开启同步时自动推送）。
- 跨设备：A 改动 → 加密推送 GitHub → B 拉取解密合并（同一密码）。

## 6. 验收标准
- [x] C 端五大模块可用（增删改查/搜索/渲染）。
- [x] C 端密码门生效，错误密码无法看数据。
- [x] GitHub 云同步在双设备一致（端到端加密、无明文泄露）。
- [x] 管理台与 C 端读写同一份加密数据，改动互通。
- [x] 断网时 C 端本地功能正常；恢复后同步正常。
- [x] 电脑（管理台）与手机（C 端）视图均可用、不串版。
- [x] 导出/导入 JSON 可完整备份恢复。
- [x] README / PRD / 技术栈文档 / 迁移指南随交付并可读。
- [ ] 财政自定义账户可增删改，记账选账户并正确计入余额。
- [ ] 财政趋势日/月/年三种粒度切换正确、图表无错位。
- [ ] 财政新增/编辑表单在手机窄屏两列不溢出、观感紧凑。
- [ ] 财政明细可按账户筛选/分组；攒钱关联账户正确显示。

## 7. UI 与风格（双端区分）
- **C 端（手机 App）**：移动优先、卡片化、年轻活泼；底部固定标签栏（看板/综合/财政/我的）；统一设计令牌与栅格（间距/层级/页头/卡片密度）；各模块针对性 icon；多视图用顶部「子 tab」组织；颜色来自 `tokens.css` 设计令牌；浅色/深色可切换。
- **B 端（管理控制台）**：桌面侧边栏 + 顶栏 + 内容区；中性专业配色、表格化、清晰层级；**响应式侧栏**（桌面可折叠图标栏 / 手机抽屉式）；浅色/深色可切换（`admin.css`）。
- 两端均不使用任何受版权保护的角色/吉祥物形象；站头以中性几何标识点缀。
- C 端配置项（站点名/主题/模块/预设/看板布局）在「我的」页或管理台「站点配置」调整，无需改代码。
- 双端共用同一会话解锁，可在不退出的情况下互相跳转（C 端「我的 → 管理台」、B 端「← 工作台」），无需重复输密码。
