# 技术栈文档 —— 个人工作台（双端 + 后端 v2.0）

## 1. 选型原则
1. 小白友好：C 端零构建工具、双击/本地服务即用；B 端/CDN 化。
2. 可维护：代码直白，用户自己能改文字/样式。
3. 隐私安全：数据本地优先 + 端到端加密（密码不离开浏览器）。
4. 跨设备：账号云同步（后端存储加密 blob）。
5. 低成本：后端零原生依赖（文件存储，可换 SQLite）。

## 2. 技术栈总表
| 类别 | 选型 | 用途 | 端/位置 |
|---|---|---|---|
| 前端结构 | 原生 HTML5（多页） | C 端站点骨架 | 本地文件 |
| 样式（C 端） | 原生 CSS3 + 设计令牌 `tokens.css` | 年轻化移动 App 风 | 本地文件 |
| 样式（B 端） | 原生 CSS3 `admin.css` | 专业后台风 | 本地文件 |
| 脚本（C 端） | 原生 JavaScript (ES6) | 页面逻辑、数据层 | 本地文件 |
| 脚本（B 端） | 原生 JavaScript + Chart.js | 后台逻辑、图表 | 本地文件/CDN |
| 图表 | Chart.js | 看板/后台图表 | CDN |
| Markdown | marked.js | 笔记/内容渲染 | CDN |
| 本地存储（C 端） | localStorage / sessionStorage | 本地加密数据 / 会话解锁 | 浏览器 |
| 加密 | Web Crypto API（AES-GCM + PBKDF2） | 数据端到端加密 | 浏览器原生 |
| 后端 | Node.js + Express | 鉴权/内容/订单/同步 API | server/ |
| 鉴权 | jsonwebtoken（JWT）+ bcryptjs | 登录态与密码哈希 | server/ |
| 存储（后端） | 轻量文件 JSON 存储（零原生依赖） | 用户/内容/订单/同步 | server/data/*.json |
| 托管 | 任意静态托管 + Node 服务 | 在线访问 | 自托管 |

## 3. 各项说明与理由
- **C 端纯静态**：不需 Node/打包，本地服务即开；用户日后改文字/样式无门槛。
- **B 端原生 + Chart.js**：轻量、CDN 即用，满足后台简单图表。
- **marked.js**：把 Markdown 笔记/内容渲染成网页，体积小。
- **localStorage**：浏览器自带键值存储；sessionStorage 用于同标签页内免重复输密码。
- **Web Crypto API**：浏览器原生加密，AES-GCM 保证机密性，PBKDF2 从密码派生密钥，密码不落盘、不外传。
- **Node/Express 后端**：提供真实账号体系与内容/订单/同步接口；文件 JSON 存储零编译，生产可平滑替换为 SQLite/Postgres（接口一致）。
- **JWT + bcrypt**：无状态登录态；密码哈希存储，后端不存明文密码。
- **PWA**：C 端可「添加到手机主屏幕」像 App 使用，并支持离线。

## 4. 系统架构（三层）
```
            ┌─────────────────────────────┐
            │   B 端（管理系统 /admin）    │  桌面专业后台
            │  登录·内容管理·用户/角色/租户·运维 │
            └──────────────┬──────────────┘
                           │  HTTPS /api/admin/*
                           ▼
            ┌─────────────────────────────┐
            │  后端服务（Node/Express）     │  JWT 鉴权 · RBAC
            │  /api/auth  /api/contents    │  文件 JSON 存储
            │  /api/orders /api/sync/blob  │  （后端只见密文）
            └──────────────┬──────────────┘
                           │  公开读内容 / 用户归属写
                           ▼
            ┌─────────────────────────────┐
            │   C 端（手机 App / 根目录）   │  本地优先 · 端到端加密
            │  看板·任务·笔记·知识库·内容·商城·我的 │
            │  浏览器内 AES 加密 → blob 上传同步  │
            └─────────────────────────────┘
```
- **端到端加密**：C 端用访问密码在浏览器内 AES-GCM 加密数据，后端只存密文 blob；账号令牌仅作传输凭证，隐私不泄露。
- **内容流向**：B 端发布（写 `contents`）→ C 端公开读取；C 端本地数据经密码加密后上传 `sync/blob` 跨设备合并。

## 5. 数据模型
### 5.1 后端（server/data/*.json）
```
users:    [{ id, username, passwordHash, role, tenantId, displayName, createdAt }]
roles:    [{ id, name, perms[], createdAt }]
tenants:  [{ id, name, createdAt }]
settings: [{ id:'site', siteName, brandColor, allowRegister, updatedAt }]
contents: [{ id, title, summary, body(md), category, tags[], status:'published'|'draft', publishedAt, updatedAt }]
orders:   [{ id, userId, items[{id,title,price,qty}], total, note, status:'paid', paidAt, createdAt }]
sync:     [{ userId, blob(base64 密文), updatedAt }]   // 后端只见密文
audit:    [{ ts, actor, action, detail }]
```
### 5.2 C 端（浏览器 localStorage，整体加密进一个 blob）
```
tasks:    [{ id, title, note, due, status, tags[], updatedAt }]
notes:    [{ id, title, body(md), tags[], links[], updatedAt }]
snippets: [{ id, title, lang, code, desc, updatedAt }]
habits:   [{ id, name, checkins:"YYYY-MM-DD"[], updatedAt }]
meta:     { version, lastSyncAt }
config:   { siteName, theme, modules, defaults, dashboard, cloud, profile }  // 随数据整体加密
```

## 6. 同步与加密机制
- **加密格式（v2）**：`版本字节(2) + 16B 随机 salt + 12B IV + AES-GCM 密文`，base64 分块避免大数组溢出；旧 v1（固定 salt）数据仍可解密。
- **密钥派生**：访问密码 → PBKDF2 → AES-GCM 密钥（密码从不上传）。
- **云同步（替换原 GitHub 同步）**：C 端登录账号 → `GET/PUT /api/sync/blob` 上传/下载加密 blob；多设备用同一账号 + 同一密码解密合并，冲突以 `updatedAt` 最新者为准。
- **兜底**：随时可导出明文/密文 JSON 到本机做备份。

## 7. 部署方案
### 7.1 后端
```
cd server
npm install            # express jsonwebtoken bcryptjs
npm start              # 默认监听 http://localhost:3000
```
- 首次启动自动播种：管理员 `admin/admin123`、默认角色/租户、站点配置、4 篇演示内容。
- 静态托管：根目录提供 C 端页面，`/admin` 提供 B 端；`/server`、`/node_modules`、`.git` 被 404 守卫，源码不泄露。

### 7.2 C 端 / B 端
- 开发预览：`python -m http.server 8000`，访问 `http://localhost:8000`（C 端）与 `http://localhost:8000/admin/login.html`（B 端）。
- 生产：把根目录静态文件 + `/admin` 交给任意静态托管；`server/` 单独部署为 Node 服务（或同进程托管）。

## 8. 目录结构
```
/
├─ index.html         C 端 看板首页
├─ content.html       C 端 内容消费（公开）
├─ tasks.html         C 端 任务
├─ notes.html         C 端 笔记（Markdown）
├─ snippets.html      C 端 知识库
├─ orders.html        C 端 交易（商城）
├─ profile.html       C 端 我的（个人中心+配置+账号同步）
├─ admin/             B 端 管理系统
│  ├─ login.html  index.html
│  └─ assets/{css/admin.css, js/{common,dashboard,content,users,settings,auth}.js}
├─ assets/
│  ├─ css/{tokens.css（设计令牌）, style.css（C 端样式）}
│  └─ js/{store,common,tasks,notes,snippets,dashboard,profile,content,orders}.js
├─ server/            Node/Express 后端
│  ├─ index.js  db.js  package.json
│  ├─ middleware/auth.js
│  └─ routes/{auth,contents,orders,sync,admin}.js
├─ manifest.webmanifest  sw.js  icon.svg
├─ README.md  PRD.md  技术栈文档.md  MIGRATION_GUIDE.md
```

## 9. 第三方依赖（均走 CDN，无需安装）
- Chart.js：`https://cdn.jsdelivr.net/npm/chart.js`
- marked.js：`https://cdn.jsdelivr.net/npm/marked@12.0.0/marked.min.js`
- 后端 npm：`express`、`jsonwebtoken`、`bcryptjs`（位于 `server/`，与 C 端 CDN 无关）

## 10. 安全注意事项
- 后端密码用 bcrypt 哈希，不存明文；JWT 用于无状态鉴权。
- C 端访问密码只在浏览器内用于加/解密，**从不上传**；后端 `sync/blob` 只存密文。
- 静态托管守卫 `/server`、`/node_modules`、`.git`，避免源码/依赖泄露。
- 生产请将 `server/data/` 加入备份；可平滑替换为 SQLite/Postgres。
- 界面区分：C 端年轻化移动 App 风；B 端专业后台风；均无受版权保护的角色/吉祥物。

## 11. 配置模型（C 端网页端可改，无需改代码）
配置对象由 `store.js` 的 `DEFAULT_CONFIG` 提供，经 `getConfig()/setConfig()/updateConfig()` 读写（随数据整体加密）：
```
config = {
  siteName,
  theme:{ mode:'light'|'dark', light:{...}, dark:{...}, fontTitle, fontBody },
  modules:[ {key,label,enabled,order,type:'page'|'link'|'custom',href,icon,core?} ],
  defaults:{ taskTags[], habitItems[], kbCategories[] },
  dashboard:{ showCards[], showWeekTrend, showCategoryBreakdown },
  cloud:{ enabled },                 // 账号云同步开关（令牌存 sessionStorage）
  profile:{ userName, role }
}
```
- C 端导航由 `common.js` 的 `getNav()` 读取 `config.modules` 动态生成（含 content/orders）。
- 「我的」页（`profile.js`）提供全部配置 UI + 账号云同步登录/注册。
