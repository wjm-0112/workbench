# 技术栈文档 —— 个人工作台（纯前端 v2.1）

## 1. 选型原则
1. 小白友好：C 端零构建工具、本地服务即用；B 端/CDN 化。
2. 可维护：代码直白，用户自己能改文字/样式。
3. 隐私安全：数据本地优先 + 端到端加密（密码不离开浏览器）。
4. 跨设备：通过用户自己的 GitHub 仓库做云端同步（端到端加密 blob）。
5. 零后端、零部署成本：纯静态，可托管到任意静态服务，无需 Node 服务器。

## 2. 技术栈总表
| 类别 | 选型 | 用途 | 端/位置 |
|---|---|---|---|
| 前端结构 | 原生 HTML5（多页） | C 端站点骨架 | 本地文件 |
| 样式（C 端） | 原生 CSS3 + 设计令牌 `tokens.css` | 年轻化移动 App 风 | 本地文件 |
| 样式（B 端） | 原生 CSS3 `admin.css` | 专业后台风 | 本地文件 |
| 脚本（C 端） | 原生 JavaScript (ES6) | 页面逻辑、数据层 | 本地文件 |
| 脚本（B 端） | 原生 JavaScript + Chart.js | 后台逻辑、图表 | 本地文件/CDN |
| 图表 | Chart.js | 看板/后台图表 | CDN |
| Markdown | marked.js | 笔记渲染 | CDN |
| 本地存储 | localStorage / sessionStorage | 本地加密数据 / 会话解锁 | 浏览器 |
| 加密 | Web Crypto API（AES-GCM + PBKDF2） | 数据端到端加密 | 浏览器原生 |
| 云端同步 | GitHub Contents API | 跨设备存储加密 blob | api.github.com |
| 托管 | 任意静态托管（GitHub Pages 等） | 在线访问 | 自托管/平台 |

## 3. 各项说明与理由
- **C 端纯静态**：不需 Node/打包，本地服务即开；用户日后改文字/样式无门槛。
- **B 端原生 + Chart.js**：轻量、CDN 即用，满足后台简单图表；且与 C 端复用同一 `store.js`，无需独立数据层。
- **marked.js**：把 Markdown 笔记渲染成网页，体积小。
- **localStorage**：浏览器自带键值存储；sessionStorage 用于同标签页内免重复输密码。
- **Web Crypto API**：浏览器原生加密，AES-GCM 保证机密性，PBKDF2 从密码派生密钥，密码不落盘、不外传。
- **GitHub Contents API**：`GET/PUT /repos/{owner}/{repo}/contents/{path}`，浏览器直连（支持 CORS），认证速率 5000 次/小时；404 表示远端暂无数据；更新需携带 `sha`。数据经密码加密后才上传，GitHub 只见密文。
- **PWA**：C 端可「添加到手机主屏幕」像 App 使用，并支持离线。

## 4. 系统架构（双端 + GitHub 云）
```
            ┌─────────────────────────────┐
            │   管理台（B 端 /admin）       │  桌面本地控制台
            │   数据概览·数据管理·站点配置·关于 │  复用同一 PB 加密存储
            └──────────────┬──────────────┘
                           │  同一浏览器 localStorage / 同一访问密码
                           ▼
            ┌─────────────────────────────┐
            │   C 端（手机 App / 根目录）   │  本地优先 · 端到端加密
            │   看板·任务·笔记·知识库·我的   │  浏览器内 AES 加密
            └──────────────┬──────────────┘
                           │  HTTPS（仅在开启云同步时）
                           ▼
            ┌─────────────────────────────┐
            │   GitHub Contents API        │  仅存密文 blob
            │   /repos/{o}/{r}/contents/{p}│  PAT 作传输凭证
            └─────────────────────────────┘
```
- **端到端加密**：C 端/管理台用访问密码在浏览器内 AES-GCM 加密数据，本地只存密文 blob；云同步时也只有密文写入 GitHub。
- **双端同源**：管理台 `admin/` 通过 `<script src="../assets/js/store.js">` 复用 C 端的 `PB` 存储层，二者共用同一密码、同一份加密数据，互改互通。

## 5. 数据模型
### 5.1 浏览器 localStorage（整体加密进一个 blob：`pwb_data_v1`）
```
tasks:    [{ id, title, note, due, status, tags[], updatedAt }]
notes:    [{ id, title, body(md), tags[], links[], updatedAt }]
snippets: [{ id, title, lang, code, desc, updatedAt }]
habits:   [{ id, name, checkins:"YYYY-MM-DD"[], updatedAt }]
meta:     { version, lastSyncAt }
config:   { siteName, theme, modules, defaults, dashboard, cloud, profile }  // 随数据整体加密
```
`cloud` 配置（v2.1）：
```
cloud: { enabled:false, provider:'github', pat:'', owner:'', repo:'', path:'sync/data.json', sha:null }
```
> 模块白名单：已移除 `content` / `orders`；旧加密 blob 中的这两个模块会在 `deepMergeConfig` 时被过滤丢弃，不影响其余数据。

### 5.2 GitHub 仓库（仅存一个加密文件）
- 路径由 `cloud.path` 指定（默认 `sync/data.json`）。
- 内容为 base64 编码的加密 blob（版本字节 + salt + IV + 密文）；GitHub 与 PAT 均不可读。

## 6. 同步与加密机制
- **加密格式（v2）**：`版本字节(2) + 16B 随机 salt + 12B IV + AES-GCM 密文`，base64 分块避免大数组溢出；旧 v1（固定 salt）数据仍可解密。
- **密钥派生**：访问密码 → PBKDF2（10 万次）→ AES-GCM 密钥（密码从不上传）。
- **GitHub 云同步**：`PB.cloudPush()` 把本地加密 blob `PUT` 到仓库（带 `sha` 乐观锁）；`PB.cloudPull()` 先 `GET` 取远端 `sha` 与密文，解密后与本地按 `updatedAt` 合并。冲突以更晚更新者为准。
- **兜底**：随时可导出明文/密文 JSON 到本机做备份（「我的」或管理台「数据管理」）。

## 7. 部署方案
### 7.1 本地预览
```
python -m http.server 8000
```
访问 `http://localhost:8000`（C 端）与 `http://localhost:8000/admin/login.html`（管理台）。
> 不能直接双击 `index.html`（`file://` 下加密被禁用）。

### 7.2 生产（纯静态）
- 把根目录静态文件（html + `assets/` + `manifest.webmanifest` + `sw.js` + `icon.svg`）+ `admin/` 交给任意静态托管（GitHub Pages / Vercel / Nginx / 对象存储）。
- 无需任何服务器进程；云同步由浏览器直连 GitHub。
- GitHub Pages：仓库 Settings → Pages → Deploy from a branch → master / `/ (root)`；仓库已含 `.nojekyll`。

## 8. 目录结构
```
/
├─ index.html         C 端 看板首页
├─ tasks.html         C 端 任务
├─ notes.html         C 端 笔记（Markdown）
├─ snippets.html      C 端 知识库
├─ profile.html       C 端 我的（个人中心+配置+GitHub 同步）
├─ admin/             B 端 本地管理控制台
│  ├─ login.html  index.html
│  └─ assets/{css/admin.css, js/{common,auth,dashboard,data,config,about}.js}
├─ assets/
│  ├─ css/{tokens.css（设计令牌）, style.css（C 端样式）}
│  └─ js/{store,common,tasks,notes,snippets,dashboard,profile,github-sync}.js
├─ manifest.webmanifest  sw.js  icon.svg  .nojekyll
├─ README.md  PRD.md  技术栈文档.md  MIGRATION_GUIDE.md
```

## 9. 第三方依赖（均走 CDN，无需安装）
- Chart.js：`https://cdn.jsdelivr.net/npm/chart.js`
- marked.js：`https://cdn.jsdelivr.net/npm/marked@12.0.0/marked.min.js`

## 10. 安全注意事项
- 访问密码只在浏览器内用于加/解密，**从不上传**；GitHub 仓库只存密文。
- PAT 仅作 GitHub API 传输凭证，建议用细粒度令牌（Fine-grained）且仅授权目标仓库的 Contents 读写；本地不持久化明文 PAT（会话级使用）。
- 不在代码或公开仓库写入 PAT / 密码；任何在聊天中明文出现过的令牌，建议到 GitHub 作废并重新生成。
- Chart.js / marked.js 走 CDN，离线时图表/Markdown 渲染降级但不影响核心数据。

## 11. 配置模型（网页端可改，无需改代码）
配置对象由 `store.js` 的 `DEFAULT_CONFIG` 提供，经 `getConfig()/setConfig()/updateConfig()` 读写（随数据整体加密）：
```
config = {
  siteName,
  theme:{ mode:'light'|'dark', light:{...}, dark:{...}, fontTitle, fontBody },
  modules:[ {key,label,enabled,order,type:'page'|'link'|'custom',href,icon,core?} ],
  defaults:{ taskTags[], habitItems[], kbCategories[] },
  dashboard:{ showCards[], showWeekTrend, showCategoryBreakdown },
  cloud:{ enabled, provider:'github', pat, owner, repo, path, sha },
  profile:{ userName, role }
}
```
- C 端导航由 `common.js` 的 `getNav()` 读取 `config.modules` 动态生成（默认含 看板/任务/笔记/知识库/我的）。
- 「我的」页（`profile.js`）提供全部配置 UI + GitHub 云端同步；管理台「站点配置」提供同样的模块/主题开关。
