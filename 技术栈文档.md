# 技术栈文档 —— 个人工作台

## 1. 选型原则
1. 小白友好：零构建工具、无需装开发环境。
2. 可维护：代码直白，用户自己能改文字/样式。
3. 隐私安全：数据本地优先 + 端到端加密。
4. 跨设备：静态托管 + 浏览器存储 + 云同步。
5. 低成本：全部免费方案。

## 2. 技术栈总表
| 类别 | 选型 | 用途 | 方式 |
|---|---|---|---|
| 前端结构 | 原生 HTML5 | 多页面站点骨架 | 本地文件 |
| 样式 | 原生 CSS3（自定义变量） | 蜡笔卡通风、手机适配 | 本地文件 |
| 脚本 | 原生 JavaScript (ES6) | 页面逻辑、数据层 | 本地文件 |
| 图表 | Chart.js | 看板小图表 | CDN |
| Markdown | marked.js | 笔记渲染 | CDN |
| 本地存储 | localStorage / sessionStorage | 本地数据缓存 / 会话解锁 | 浏览器 |
| 加密 | Web Crypto API（AES-GCM + PBKDF2） | 数据加密/解密 | 浏览器原生 |
| 云同步 | GitHub REST API（Contents） | 读写加密数据文件 | 用户令牌调用 |
| 托管 | GitHub Pages | 在线访问站点 | 免费 |
| 可安装 | PWA（manifest + Service Worker） | 手机添加到主屏、离线 | 本地文件 |

## 3. 各项说明与理由
- 纯静态（HTML/CSS/JS）：不需 Node/打包，双击即开；部署 GitHub Pages 零成本；用户日后改起来没门槛。
- Chart.js：轻量、CDN 即用，满足看板简单图表。
- marked.js：把 Markdown 笔记渲染成网页，体积小。
- localStorage：浏览器自带键值存储，无需后端，适合个人小数据；sessionStorage 用于同标签页内免重复输密码。
- Web Crypto API：浏览器原生加密，AES-GCM 保证机密性，PBKDF2 从密码派生密钥，密码不落盘、不外传。
- GitHub 同步：用 GitHub 免费仓库存「加密后的数据文件」，通过官方 Contents API 读写；用户只需免费账号 + 一把最小权限令牌。
- PWA：让用户把网站「添加到手机主屏幕」像 App 使用，并支持离线。

## 4. 系统架构（数据流向）
```
用户输入 → 页面 JS → store.js（数据层）
              ├─ 本地：写入 localStorage（即时，AES 加密）
              └─ 云端（开启后）：AES 加密 → GitHub Contents API → 私有仓库 data.json
其他设备打开：GitHub 拉取 data.json → AES 解密（同一密码）→ 合并到 localStorage → 渲染
```
- 密码只在本地用于加/解密，从不上传。
- 令牌只存本机 localStorage，仅用于读写自己的仓库。

## 5. 数据模型
```
tasks:    [{ id, title, note, due, status, tags[], updatedAt }]
notes:    [{ id, title, body(md), tags[], links[], updatedAt }]
snippets: [{ id, title, lang, code, desc, updatedAt }]
habits:   [{ id, name, checkins: "YYYY-MM-DD"[], updatedAt }]
meta:     { version, lastSyncAt }
```
所有模块数据汇总进一个 `data.json`，整体加密后存储。

## 6. 同步与加密机制
- 加密：密码 → PBKDF2 派生密钥 → AES-GCM 加密整个 data 对象 → base64 密文（含随机 IV）。
- 存储位置：密文写入 GitHub 仓库的 `data.json`（私有仓库；公开仓库亦可，因已加密）。
- 同步时机：本地改动防抖自动上传；页面加载时拉取远程并解密合并；冲突以 `updatedAt` 最新者为准。
- 兜底：随时可导出明文/密文 JSON 到本机做备份。

## 7. 部署方案
1. 在 GitHub 新建仓库（建议 Private），开启 GitHub Pages。
2. 把站点文件推上去（或按引导上传）。
3. 生成仅「contents: read/write」的 Fine-grained PAT，粘进网站设置。
4. 手机浏览器打开 Pages 地址 → 添加到主屏幕。
> 不配置 GitHub 也能用本地版（localStorage）。

## 8. 目录结构
```
/
├─ index.html        首页 / 数据看板
├─ tasks.html        任务待办（表格化）
├─ notes.html        笔记资料（TOC/搜索高亮）
├─ snippets.html     知识库（结构化卡片）
├─ profile.html      我的（个人中心 + 全部配置）
├─ assets/
│  ├─ css/style.css
│  └─ js/{store,common,tasks,notes,snippets,dashboard,profile}.js
├─ manifest.webmanifest
├─ sw.js
├─ icon.svg
├─ README.md
├─ PRD.md
└─ 技术栈文档.md
```

## 9. 第三方依赖（均走 CDN，无需安装）
- Chart.js（图表）：`https://cdn.jsdelivr.net/npm/chart.js`（离线时图表区显示提示，不影响其余功能）

## 10. 安全注意事项
- PAT 仅授予最小权限（读写仓库内容），不授予其他范围。
- 密码请牢记；一旦遗忘，加密数据不可恢复。
- 建议保留至少一份导出备份（JSON）。
- 公共网络下注意设备本身安全（网站无后端，无服务器留存你的数据）。
- **配置与同步凭据一并进入加密 blob**：`data.config`（站点名/主题/模块/预设/看板布局/同步令牌等）随数据整体 AES-GCM 加密存储与同步，浏览器 localStorage 中只见密文，不再有明文 `pwb_settings_v1`。
- 界面为专业管理后台风格（深蓝主色、中性配色、浅色/深色可切换）；原卡通涂鸦装饰已移除，无任何受版权保护的角色或吉祥物形象。

## 11. 配置模型（网页端可改，无需改代码）
配置对象 `data.config` 由 `store.js` 的 `DEFAULT_CONFIG` 提供，经 `getConfig()/setConfig()/updateConfig()` 读写，结构：
```
config = {
  siteName,                       // 站点名称
  theme:{ mode:'light'|'dark', light:{...}, dark:{...}, fontTitle, fontBody },
  modules:[ {key,label,enabled,order,type:'page'|'link'|'custom',href,icon,core?} ],
  defaults:{ taskTags[], habitItems[], kbCategories[] },
  dashboard:{ showCards[], showWeekTrend, showCategoryBreakdown },
  sync:{ enabled, user, repo, token, path },     // 加密存储
  profile:{ userName, role }
}
```
- 导航由 `common.js` 的 `getNav()` 读取 `config.modules` 动态生成（过滤 enabled、按 order 排序）。
- 「我的」页（`profile.js`）提供全部配置 UI；新增模块仅支持 `link`（外链）/ `custom`（内部入口）类型，复杂富模块仍需开发（预留能力）。
